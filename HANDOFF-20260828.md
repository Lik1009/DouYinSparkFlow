# DouYinSparkFlow 交接文件（2026-08-28）

> **⚠️ 2026-08-28 更新：本文件已降级为"事故存档"**。08-27 事故后确立的"新会话硬化期"理论
> 经复核证据不足（08-02~08-25 自然间隔 14-39h 从未触发该条件），已按早期可验证风格**回退**
> （commit 920aba0：catchup 硬化期保护删除、keepalive.js 标记删除、仓库变量删除；ai-news-douyin 已弃用，
> 会话同步改为两处）。**现行规则一律以 AGENTS.md 为准**，本文件 §0/§3/§5/§6 中涉及硬化期/三处同步的
> 内容已过时，仅作历史复盘参考。

> 本文件是 2026-08-25 ~ 08-28 cookies 失效事故的完整复盘 + 当前状态快照 + 接手待办 + 操作手册。
> 长期架构/核心逻辑/选择器/历史踩坑请看 **AGENTS.md**（本文件只写增量与最新状态，不重复）。
> 接手 agent：先读 AGENTS.md §11 的 Prompt（现行版），再按需翻阅本文件历史细节。

---

## 0. 给接手 agent 的 Prompt（复制即用）

```text
你在接手一个抖音火花自动续火项目（仓库 Luolingli/DouYinSparkFlow，工作目录 .../DouYinSparkFlow）。
项目由 GitHub Actions 每天北京时间 00:40（cron "40 16 * * *" UTC）给 10 个好友各发一条"今日火花"消息，维持火花。
动手前必须通读仓库根目录 AGENTS.md（长期文档）和 HANDOFF-20260828.md（最近事故复盘+当前待办），
重点：核心逻辑 core/tasks.py（搜索直达+滚动兜底）；浏览器必须 timezone_id=Asia/Shanghai；
当天去重基于聊天面板消息时间（北京时间解析）；同一会话全账号唯一（§12）。

【最高优先级规则：新会话硬化期】（2026-08-28 事故后确立，违反 = cookies 被抖音风控杀死）
- 扫码后 6 小时内，禁止任何 GitHub（美国 runner）工作流访问 cookies（主发送/catchup/keepalive/手动 dispatch）。
- catchup 已内置保护（读仓库变量 LAST_RESCAN_UTC，距扫码 <6h 拒绝 dispatch），但手动操作必须自觉遵守。
- 安全扫码窗口：北京时间 18:40 之后扫码（保证次日 00:40 发送距扫码 ≥6h）。
  白天扫码可以，但当天 00:40 发送必然失败或杀会话，当天火花只能靠保护期后的 catchup 兜底（不保证）。
- 扫码后不要立刻 Approve 审批门禁中排队的 run（Approve 会让排队 run 立刻开跑 = 立即美国访问）。
- 规律依据：扫码后 ≤3.4min 美国访问 4/4 死亡（S1~S5）；≥6.2h 首次美国访问有 2 个存活先例（8 天/数周）。
  机制是推断不是铁证，但所有操作必须按最保守边界（6h）执行。

【cookies 三处同步】（§12 单会话规则，共享账号 Sakuro_Mai）
- 本地 ~/.config/douyin_keepalive/cookies.json ↔ GitHub secret COOKIES_SAKURO_MAI（env user-data）↔ ai-news-douyin/data/web_cookies.json
- 本地扫码（bash ~/.config/douyin_keepalive/run.sh）自动完成前两处 + 写 LAST_RESCAN_UTC 标记；
  第三处（ai-news-douyin）必须手动复制（脚本见 HANDOFF §6.4，保留 sameSite 字段映射，先备份原文件）。
- 任何一方（含 ai-news-douyin 项目）重新扫码都会使旧会话立即失效；日常 keepalive 心跳不会。

【硬性约束（用户明确要求，不得违反）】
1. 调试/测试发送只允许发给 zjj00000010 和 84611333990 两个人，禁止发给其他好友。
   方式：临时设仓库变量 DEBUG_TARGETS='["zjj00000010","84611333990"]' + DEBUG_BYPASS_DEDUP=1，用完必须删除。
2. 不要修改 .github/workflows/schedule.yml，除非用户明确要求（触发 GitHub 审批门禁，需用户手动 Approve）。
3. 不要打印/泄露 COOKIES_SAKURO_MAI 的值（也不要在日志/文档里出现）。
4. 不对真实账号做未经用户确认的多轮/大批量发送；不自行反复手动触发生产发送。
5. 操作前向用户确认要解决的问题；重大操作（扫码、改时间、触发生产发送）先征得同意。

【验证与常用命令】
- 每天成功标志：日志 "任务完成，共发送 10 条消息，跳过 0 条" + review 输出 "10/10 检查通过"
- gh run list --repo Luolingli/DouYinSparkFlow --limit 10
- gh run view --repo Luolingli/DouYinSparkFlow <id> --log
- 主发送手动触发：gh workflow run "DouYin Spark Flow Schedule Run" --repo Luolingli/DouYinSparkFlow
- keepalive：gh workflow run "DouYin Cookie Keepalive" --repo Luolingli/DouYinSparkFlow
- catchup：gh workflow run "DouYin Send Catchup" --repo Luolingli/DouYinSparkFlow
- 硬化期标记：gh api repos/Luolingli/DouYinSparkFlow/actions/variables/LAST_RESCAN_UTC --jq .value
- 本地扫码：bash ~/.config/douyin_keepalive/run.sh（弹二维码，15 分钟有效；launchd 每 6h 自动跑一次）
- 工件下载：gh run download <id> --repo Luolingli/DouYinSparkFlow -n run-logs
```

---

## 1. 当前状态快照（2026-08-28 10:50 北京时间，接手时先按 §5 重新核对）

| 项 | 状态 |
|---|---|
| 发送时间 | cron `40 16 * * *` UTC = 北京 00:40（08-28 由 03:00 改，commit 425b765） |
| 审批门禁 | schedule.yml 被隔离后，用户已于 08-28 10:31 Approve；workflow state=active，cron 应已恢复（00:40 时点当天已触发过一次，延迟 8.4h 到 09:05 才跑） |
| cookies 会话 | **S5（08-28 10:31 扫码）已死**——10:31:57 启动的排队 run 首次美国访问（+3.4min）被风控杀，0 消息发出；keepalive 复核确认；ai-news-douyin 已排除嫌疑 |
| 用户待办 | **今晚 16:00-17:30 重扫**（提醒任务已挂 15:50）。扫后 08-29 00:40 发送距扫码 ≥7h（安全） |
| 08-28 火花 | 未发。16:31 后若 catchup 漂移触发可抢救（08-27 曾漂移到 20:50/23:13，有概率），不保证 |
| 08-26/08-27 火花 | 已漏（事故期间，无法补救） |
| 本地 launchd | `com.luolingli.douyin-sparkflow-keepalive` 已加载（每 6h 无头续期/失效弹二维码，RunAtLoad=false，PATH 含 ~/.local/bin/gh，日志在 ~/.config/douyin_keepalive/launchd.*.log） |
| 硬化期保护 | catchup 已内置并生产实测（commit 051396d）；keepalive.js 扫码后自动写 LAST_RESCAN_UTC |
| 目标好友 | 10 人（08-19 新增 72534209781；08-24 最后一次 10/10 全发） |
| 仓库变量 | 无（LAST_RESCAN_UTC 随扫码出现；调试变量必须用完即删） |

---

## 2. 事故时间线（2026-08-25 ~ 08-28，北京时间）

| 时间 | 事件 |
|---|---|
| 08-25 03:33 | 最后一次成功发送（10/10，含新好友） |
| 08-25 21:36 | keepalive 首次报"登录已失效"（08-17 建立的会话自然到期，约 8 天寿命） |
| 08-26 03:33 | 主发送失败：未登录（0 消息） |
| 08-26 11:26 | catchup 首次真实兜底，暴露 `gh workflow run` 缺 `--repo` bug → 修复 a91708c 并实测 |
| 08-27 19:03 | 用户扫码 S1 → 19:05 美国 keepalive（+2min）当时有效 → 19:12 主发送（+9min）未登录：**S1 死** |
| 08-27 19:18 | 用户扫码 S2 → 19:20 美国主发送（+1.5min）登录通过、列表渲染出 5 个群 → 19:24 本地检查已死：**S2 死**（首次访问能工作、随后被杀 = 风控延迟生效特征） |
| 08-27 20:50/23:13 | catchup cron 漂移（计划是 10:17/12:17/15:17）两次触发 dispatch，均失败（会话已死，0 消息） |
| 08-27 22:00 | 用户扫码 S4 → 23:13 漂移 catchup dispatch（+73min）未登录：**S4 死** |
| 08-28 09:05 | 主发送 cron（16:40Z 时点）延迟 8.4h 触发，run 排队（审批门禁隔离中） |
| 08-28 10:17 | catchup 准时触发，dispatch 被门禁按 run 拦截（action_required，未执行） |
| 08-28 10:31:13 | 用户扫码 S5 + 顺手 Approve → 排队 run 10:31:57 立刻开跑（捕获 S5 cookies） |
| 08-28 10:34:56 | 该 run 未登录失败（S5 首次美国访问 +3.4min 被杀，0 消息）；10:36 keepalive 复核确认 S5 死 |
| 08-28 10:40 | 排查排除 ai-news-douyin（两边 sessionid 一致，仅我方同步动过）；文档补全 283ec59 |

**根因链**：会话自然到期（不可避免，约 8 天寿命，只能扫码续期）+ 恢复操作踩了"新会话硬化期"（扫码后数分钟内美国访问 = 风控杀会话，当时未知）+ catchup 的 cron 漂移把风险放大。**非代码 bug**（应用层去重/硬上限/快速失败全部正常，全程 0 误发 0 重复）。

---

## 3. 核心规律：新会话硬化期（推断，按 6h 保守边界执行）

**数据点**（全部来自本仓库 run 历史 + 本地 keepalive.log）：

| 会话 | 首次美国访问 | 结果 |
|---|---|---|
| S1（08-27 19:03 扫） | +2min（keepalive）有效，+7min 未登录 | ❌ 死 |
| S2（08-27 19:18 扫） | +1.5min（主发送）登录通过，+6min 本地检查死 | ❌ 死 |
| S4（08-27 22:00 扫） | +73min（漂移 catchup dispatch）未登录 | ❌ 死 |
| S5（08-28 10:31 扫） | +3.4min（排队 run）连登录检查都未通过 | ❌ 死 |
| 08-02 首扫 | +26.4h（首次 keepalive 15:21Z 08-03） | ✅ 存活 |
| 08-17 更新 | ≥+6.2h（21:11 首次成功 keepalive） | ✅ 存活 8 天（每天 4 次美国访问+1 次发送） |

- 死亡区 ≤3.4min 实测 4/4；存活证据 ≥6.2h；机制不确定（可能是"新会话+异地数据中心访问"触发、延迟生效），不要试图缩小等待时间。
- 老会话（存活 >1 天）对每日多次美国访问免疫（8 天 × 5 次/天 无事故）——所以日常运行无需限制，限制只针对**扫码后的新会话**。
- 落地对策：① keepalive.js 扫码后写 `LAST_RESCAN_UTC` 仓库变量；② catchup 读该变量，<6h 拒绝 dispatch（实测通过）；③ 操作规则：安全扫码窗口北京 18:40 后；白天扫码靠保护期兜底。

---

## 4. 本次事故修复清单（全部已推送 main，除标注"本地"外）

| commit | 内容 |
|---|---|
| a91708c | catchup `gh workflow run` 加 `--repo Luolingli/DouYinSparkFlow`（无 checkout 步骤时必需） |
| f8cd56a | AGENTS.md §6.11 记录 --repo 坑 |
| 425b765 | schedule.yml cron `0 19 * * *` → `40 16 * * *`（用户要求，北京 00:40）+ AGENTS.md 同步 |
| 051396d | catchup 硬化期保护（LAST_RESCAN_UTC <6h 不 dispatch）+ AGENTS.md §6.12 |
| 283ec59 | AGENTS.md §6.12 补充：S5 数据、审批门禁交互坑、安全扫码窗口 |
| （本地） | keepalive.js saveAndPublish 增加 LAST_RESCAN_UTC 标记写入（容错：失败只告警不影响 cookies） |
| （本地） | launchd plist 重建：~/Library/LaunchAgents/com.luolingli.douyin-sparkflow-keepalive.plist（StartInterval 21600，RunAtLoad false，PATH=/Users/luolingli/.local/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin） |
| （本地） | ai-news-douyin/data/web_cookies.json 两次同步 S2/S5（备份 .bak-20260827 / .bak-20260827b；S5 为当前值） |

---

## 5. 接手后核对清单（按顺序执行）

1. `date`（确认北京时间，一切判断的基准）
2. `gh run list --repo Luolingli/DouYinSparkFlow --limit 12` —— 最近运行是否符合预期（无意外 in_progress、无连续失败）
3. `gh api repos/Luolingli/DouYinSparkFlow/actions/variables/LAST_RESCAN_UTC --jq .value` —— 距现在几小时？
   - **<6h（硬化期内）**：静默观察，不触发任何使用 cookies 的工作流，等保护期过
   - ≥6h 或变量不存在：继续
4. 会话死活：最近一次 keepalive run 是否"会话有效"？（失败=死）
5. 三处一致性：本地 cookies.json mtime、ai-news-douyin/data/web_cookies.json 的 sessionid 与本地是否一致（不一致=某方单独扫码过，需重新对齐）
6. 审批门禁：`gh api repos/Luolingli/DouYinSparkFlow/actions/workflows/schedule.yml --jq .state` 应为 active；且最近一个 16:40Z（00:40 北京）时点应有 schedule run（没有=门禁又拦了，提醒用户 Approve）
7. 会话死 → 提醒用户在**安全窗口（北京 18:40 后，最好 19:00-21:00）**扫码；扫完按 §6.3 同步+验证
8. 一切恢复后：确认 10/10 成功发送 + review 通过，向用户报告，更新本文件 §1 状态快照

## 6. 操作手册

### 6.1 触发工作流（注意硬化期！扫码 <6h 内禁止）
```bash
gh workflow run "DouYin Spark Flow Schedule Run" --repo Luolingli/DouYinSparkFlow   # 主发送
gh workflow run "DouYin Cookie Keepalive" --repo Luolingli/DouYinSparkFlow          # keepalive（也是会话死活测试）
gh workflow run "DouYin Send Catchup" --repo Luolingli/DouYinSparkFlow              # catchup（硬化期内会自行跳过 dispatch）
```

### 6.2 本地扫码（用户在场时）
```bash
bash ~/.config/douyin_keepalive/run.sh   # 会话有效→无头续期 42s；失效→弹二维码窗口（15 分钟）
```
扫码成功后自动：本地 cookies.json + GitHub secret + LAST_RESCAN_UTC 标记。日志：~/.config/douyin_keepalive/keepalive.log。
注意：脚本可能因后台进程节流延迟几十秒~几分钟才弹窗口（08-27 实测延迟 14 分钟），属正常。

### 6.3 扫码后同步 ai-news-douyin（必须，§12）
```bash
python3 - <<'PY'
import json, shutil
a = json.load(open("/Users/luolingli/.config/douyin_keepalive/cookies.json"))
b = json.load(open("/Users/luolingli/Documents/work/ai-news-douyin/data/web_cookies.json"))
samesite = {c["name"]: c.get("sameSite", "Lax") for c in b}
out = []
for c in a:
    c2 = dict(c)
    c2["sameSite"] = samesite.get(c["name"], "Lax")
    out.append(c2)
path = "/Users/luolingli/Documents/work/ai-news-douyin/data/web_cookies.json"
shutil.copy(path, path + ".bak")
json.dump(out, open(path, "w"), ensure_ascii=False, indent=2)
sess = [c["name"] for c in a if c["name"] in ("sessionid","sessionid_ss","sid_guard","sid_tt")]
print("同步", len(out), "个；会话 cookies:", sess)
PY
```

### 6.4 硬化期标记管理
```bash
# 查看
gh api repos/Luolingli/DouYinSparkFlow/actions/variables/LAST_RESCAN_UTC --jq .value
# 手动设置（通过其他途径更新 cookies 时）
gh variable set LAST_RESCAN_UTC --repo Luolingli/DouYinSparkFlow --body "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
# 删除（缺失时 catchup 按原逻辑 dispatch）
echo y | gh variable delete LAST_RESCAN_UTC --repo Luolingli/DouYinSparkFlow
```

### 6.5 launchd 本地续期任务管理
```bash
launchctl print gui/$(id -u)/com.luolingli.douyin-sparkflow-keepalive | head -8   # 查看
launchctl bootout gui/$(id -u)/com.luolingli.douyin-sparkflow-keepalive          # 卸载
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.luolingli.douyin-sparkflow-keepalive.plist  # 加载
cat ~/.config/douyin_keepalive/keepalive.log    # 续期/扫码历史
```
合盖睡眠期间任务不跑，开盖后补跑一次（不堆积）；GitHub 端 keepalive 在合盖期间维持会话。

### 6.6 审批门禁（schedule.yml 被隔离时）
- 表现：workflow API state 仍可能是 active，但 cron 不触发、dispatch 变 action_required
- 解除：用户到 https://github.com/Luolingli/DouYinSparkFlow/actions 顶部横幅点 Approve（或 Settings → Code security）
- 验证：Approve 后观察下一个 cron 时点是否有 schedule run 出现
- ⚠️ 排队中的 run 会在 Approve 时立刻开跑并捕获**当时**的 cookies——所以**刚扫码时绝不要 Approve**

### 6.7 调试发送（仅用户明确要求时）
```bash
gh variable set DEBUG_TARGETS --repo Luolingli/DouYinSparkFlow --body '["zjj00000010","84611333990"]'
gh variable set DEBUG_BYPASS_DEDUP --repo Luolingli/DouYinSparkFlow --body '1'
# 触发 → 验证 → 必须清理：
echo y | gh variable delete DEBUG_TARGETS --repo Luolingli/DouYinSparkFlow
echo y | gh variable delete DEBUG_BYPASS_DEDUP --repo Luolingli/DouYinSparkFlow
```

---

## 7. 环境坑速查（接手 agent 会遇到的）

1. **GitHub cron 漂移严重**：08-27 catchup 计划 10:17/12:17/15:17 实际 20:50/23:13 触发；08-28 主发送 00:40 时点延迟 8.4h。不要假设任何"准时"，一切以 run 列表为准。
2. **GitHub API quirk**：通用端点 `/actions/runs?workflow_id=...` 的过滤参数完全失效（返回全量），必须用专用端点 `/actions/workflows/<file>/runs`。
3. **时区**：GitHub created_at 是 UTC；一切"今天/当天"判断必须换算北京时间（`TZ=Asia/Shanghai`）。00:40 北京发送 = UTC 前一天 16:40，字符串比较会跨天误判。
4. **run_code（DSH harness）坑**：JS 模板字符串里插值的 `${...}` 要转义成 `\\${...}`；bash 变量不会从 JS 作用域展开；整个 run_code 调用有 600s 墙钟（含模型延迟），长 sleep 用 240s 以内更稳；工具参数名是 `description` 不是 `decription`（容易打错）。
5. **gh 偶发 EOF/空输出**：短 sleep 后重试即可。
6. **jq 复杂表达式**：shell 引号易被吞，改用 `python3 -c` 或简单 `--jq '.a, .b'`。
7. **secret 值禁止打印**：gh secret/api 输出里可能带 cookies，grep 过滤时避开；日志/文档/对话中一律不出现。
8. **git 提交身份**：本地未配置 global identity，会警告（董宝凡 <luolingli@MacBook-Air.local>），无害，不要 amend。

---

*维护：状态变化（扫码、cron 调整、修复）后更新本文件 §1/§4/§5 并 commit+push。本文件与 AGENTS.md 冲突时以时间新者为准。*
